fn main() {
    println!("--- System Check Initiated ---");

    // 1. Verify basic variable assignment and math
    let a = 10;
    let b = 32;
    let sum = a + b;

    // 2. Verify string formatting works
    println!("Math verification: {} + {} = {}", a, b, sum);

    // 3. Final success message
    println!("OK!");
}